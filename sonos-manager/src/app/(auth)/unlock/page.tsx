import { PinUnlock } from '@/components/auth/pin-unlock';

export default function UnlockPage() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-base p-4">
      <PinUnlock />
    </main>
  );
}
